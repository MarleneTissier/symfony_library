{% extends 'base.html.twig' %}
{% block content %}

        <div class="content">
                <div class="book">
                        <!--le titre du livre-->
                        <h2>{{ book.title }}</h2>
                        <!--les info du livre-->
                        <p>type : {{ book.genre }} - {{ book.nbPages }} pages</p>
                </div>
        </div>

{% endblock content %}